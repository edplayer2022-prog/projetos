{
  "name": "fluencyai",
  "private": true,
  "scripts": {
    "test": "npm run test:unit && playwright test",
    "test:unit": "node --test tests/learning-core.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
